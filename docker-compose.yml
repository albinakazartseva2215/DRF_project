#version: "3.9"

services:
  web:
    image: ${DOCKER_HUB_USERNAME}/myapp-web:${IMAGE_TAG}  # Используем образ из реестра
    command: sh -c "python manage.py collectstatic --noinput && gunicorn config.wsgi:application --bind 0.0.0.0:8000"
    volumes:
      - static_volume:/app/staticfiles  # Только статика
    ports:
      - "8000:8000"
    depends_on:
      - db
      - redis
    env_file:
      - .env

  nginx:
    build:
      context: ./nginx
    ports:
      - "80:80"
    volumes:
      - ~/nginx/nginx.conf:/etc/nginx/nginx.conf
      - static_volume:/app/staticfiles
    depends_on:
      - web

  celery:
    image: ${DOCKER_HUB_USERNAME}/myapp-celery:${IMAGE_TAG}  # Используем образ из реестра
    command: >
      sh -c "celery -A config worker -l INFO --pool=solo"
    env_file:
      - .env
    depends_on:
      - redis

  celery_beat:
    image: ${DOCKER_HUB_USERNAME}/myapp-celery:${IMAGE_TAG}  # Используем образ из реестра
    command: >
      sh -c "celery -A config beat -l INFO --scheduler django_celery_beat.schedulers:DatabaseScheduler"
    env_file:
      - .env
    depends_on:
      - redis

  db:
    image: postgres:17
    volumes:
      - postgres_data:/var/lib/postgresql/data
    env_file:
      - ./.env
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres" ]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7
    volumes:
      - redis_data:/data
    env_file:
      - ./.env

volumes:
  postgres_data:
  static_volume:
  redis_data:
